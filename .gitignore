# CDK
cdk.out/
.cdk.staging/

# Python
.venv/
.python-version
__pycache__/
*.pyc
*.pyo
.eggs/
*.egg-info/
dist/
build/

# Lambda build output (generated by scripts/build-and-deploy.sh)
build/

# Lambda build artifacts
lambda/**/package/
lambda/**/*.zip

# CloudFormation generated artifacts — contain S3 presigned URLs, never commit
template-packaged.yaml

# Claude Code local overrides — may contain personal paths or API URLs
.claude/settings.local.json

# Env
.env
.env.*
